{
  "name": "pay402-backend",
  "version": "1.0.5",
  "description": "x402 Payment Protocol Backend for PAY402 Token",
  "main": "dist/index.js",
  "scripts": {
    "dev": "cd backend && tsx watch src/index.ts",
    "prebuild": "echo 'Building PAY402 Backend v1.0.5...'",
    "build": "cd backend && npm install && npm run build",
    "start": "cd backend && npm start",
    "test": "cd backend && npm test"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "ethers": "^6.12.0",
    "axios": "^1.7.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/cors": "^2.8.17",
    "@types/node": "^20.12.7",
    "tsx": "^4.7.2",
    "typescript": "^5.4.5"
  }
}